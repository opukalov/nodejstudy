var phrases;

exports.connect = function(){
    phrases = require('./ua');
}

exports.getPhrase = function(name){
    if(!phrases[name]){
        throw new Error("phrases not exists " + name);
    }else{
        return phrases[name];
    }
}